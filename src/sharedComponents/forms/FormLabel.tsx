import React from 'react';
import styled from 'styled-components';

export const FormLabel = styled.p`
    width: 100%;
    font-size: 1.2em;
    margin: ${({ theme }) => theme.spacing.s} 0 ${({ theme }) => theme.spacing.s} 0;
`;
